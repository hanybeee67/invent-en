---
description: Push current changes to GitHub
---

1. Add all changes
    ```
    git add .
    ```

2. Commit changes
    ```
    git commit -m "Update via Antigravity Workflow"
    ```
    
3. Push to origin
    ```
    git push
    ```
